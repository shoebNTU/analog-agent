The GF180MCU-D PDK is not bundled here.

Run `notebooks/setup_and_run_gf180_analog.ipynb` from the IIC-OSIC-TOOLS
container-backed Jupyter kernel. The notebook creates:

```text
CircuitCollector/CircuitCollector/PDK/gf180mcuD
```

as a symlink to the GF180MCU-D PDK path inside that container.
